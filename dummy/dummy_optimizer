#pragma once

namespace tiny {
    /** The dummy optimizer just provides the optimized() function that can be used with any intermediate representation and does nothing to it. 
     */
    class dummy_optimizer {
    public:
        template<typename T>
        void optimize(T & il) { }
    }; 
}